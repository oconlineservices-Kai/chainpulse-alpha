# This workflow has been DEPRECATED.
# ChainPulse Alpha deploys exclusively via Fly.io.
# See fly-deploy.yml for the active deployment pipeline.
#
# Vercel deployment was removed because:
# - We use Fly.io as our single production platform
# - Vercel was causing duplicate deployment failures
# - Fly.io handles our Docker-based full-stack deployment better
#
# To deploy: push to main branch → fly-deploy.yml triggers automatically.
